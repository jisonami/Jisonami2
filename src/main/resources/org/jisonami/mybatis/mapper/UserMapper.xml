<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.jisonami.mybatis.mapper.UserMapper">

	<insert id="save" parameterType="User">
		<selectKey keyColumn="id" keyProperty="id" resultType="java.lang.String" order="BEFORE">
			select sys_guid() from dual
		</selectKey>
		insert into t_user(id, name, password) values(#{id}, #{name}, #{password})
	</insert>
	
	<delete id="delete" parameterType="java.lang.String">
		delete from t_user where id = #{id}
	</delete>
	
	<update id="edit" parameterType="User">
		update t_user set name = #{name}, password = #{password} where id = #{id}
	</update>
	
	<select id="validate" parameterType="User" resultType="java.lang.String" >
		select id from t_user where name = #{name} and password = #{password}
	</select>
	
	<select id="exits" parameterType="java.lang.String" resultType="java.lang.String">
		select id from t_user where name = #{name}
	</select>

</mapper>


